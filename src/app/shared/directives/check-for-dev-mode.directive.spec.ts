import { CheckForDevModeDirective } from './check-for-dev-mode.directive';

describe('CheckForDevModeDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckForDevModeDirective();
    expect(directive).toBeTruthy();
  });
});
